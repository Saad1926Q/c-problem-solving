#include <stdio.h>
#include <stdlib.h>

// Problem: Given an array of integers, find all duplicate elements. Do it in O(n) time and O(1) space if possible.
// Input: int arr[] = {4, 3, 2, 7, 8, 2, 3, 1};
// Output: Duplicates are 2, 3

